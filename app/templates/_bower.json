{
    "name": "<%= _.slugify(appName) %>",
    "version": "0.0.0",
    "dependencies": { <%
        if (!compassBootstrap) { %>
                "bootstrap": "~3.0.0",
            <%
        } %> <%
        if (compassBootstrap) { %>
                "sass-bootstrap": "~3.0.0",
            <%
        } %>
            "jquery": "~1.9.0"
    },
    "devDependencies": {}
}